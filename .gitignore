node_modules/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

# Local service state (used when Docker is unavailable)
.local/
dump.rdb
# Anchored to the API's own runtime upload dir — an unanchored "storage/"
# would also match apps/api/src/adapters/storage/, which is source code.
apps/api/storage/

# Playwright
test-results/
playwright-report/
blob-report/

# Local Claude Code session config — machine-specific, not project source.
.claude/
