{
  "name": "@fenwick/payment",
  "version": "0.1.0",
  "private": true,
  "description": "Public payment application. Anonymous, hardened, minimal dependency tree.",
  "scripts": {
    "dev": "node ../../scripts/with-env.mjs next dev --port '${PAYMENT_PORT:-3001}'",
    "build": "next build",
    "start": "next start --port '${PAYMENT_PORT:-3001}'",
    "typecheck": "tsc --noEmit",
    "lint": "next lint"
  },
  "//": "EVERY dependency added here is a PCI scope question (TDD-001 §3.3). This list is reviewed as a security artefact, not a build detail. Do not add a package without saying why in the pull request.",
  "//stripe": "@stripe/stripe-js + @stripe/react-stripe-js: Stripe Elements collects card data directly into Stripe's own iframe, so it never touches this app's JS context or our server — this is what keeps PCI scope at SAQ A instead of SAQ D.",
  "dependencies": {
    "@fenwick/shared": "workspace:*",
    "@stripe/react-stripe-js": "^2.9.0",
    "@stripe/stripe-js": "^4.10.0",
    "next": "^15.1.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  }
}
